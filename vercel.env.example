# Vercel Environment Variables Template
# Copy these variables to your Vercel project settings under Environment Variables

# Database Connection
DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require

# JWT Secret Key (generate a strong random string)
JWT_SECRET=your-super-secure-jwt-secret-key-here-make-it-long-and-random

# Node Environment
NODE_ENV=production

# App Configuration
NEXTAUTH_URL=https://your-vercel-domain.vercel.app
NEXTAUTH_SECRET=your-nextauth-secret-here

# Optional: Custom API Base URL
VITE_API_BASE_URL=https://your-vercel-domain.vercel.app/api

# Optional: Prisma Settings for serverless
PRISMA_CLI_BINARY_TARGETS=native,rhel-openssl-1.0.x

# Example Production Database URLs (replace with your actual database)
# For Neon (recommended):
# DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/neondb?sslmode=require

# For PlanetScale:
# DATABASE_URL=mysql://username:password@aws.connect.psdb.cloud/database?sslaccept=strict

# For Railway:
# DATABASE_URL=postgresql://username:password@containers-us-west-xxx.railway.app:port/railway

# For Supabase:
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
